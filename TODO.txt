Parsing Location 


Faire un docker nginx avec le meme fichier de config que nous
    Tester les permissions requises par le directory root


Parser le host, si host = 127.0.0.1, servername = localhost